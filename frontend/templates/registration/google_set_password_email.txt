{# DRAFT COPY -- Raymond will rewrite this before it ships. See #1237. #}
Greetings {% if user.get_full_name %}{{ user.get_full_name }}{% else %}{{ user }}{% endif %},

You're getting this email because your {{ site_name }} account signs in with
Google, and we're retiring the "Sign in with Google" button. Your account
and everything in it (pledges, library, wishlist) is staying exactly as it
is -- you just need a password so you can still get in afterward.

Set one here:

{{ reset_url }}

Your username, in case you've forgotten: {{ user.username }}

If you don't do anything, your account isn't deleted or locked -- but you
won't be able to log in until you set a password using the link above (or
the "Forgot your password?" link on the sign-in page, any time).

Questions? Just reply to this email.

Best,
{{ site_name }}
